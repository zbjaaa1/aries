#!/bin/sh

if [ "$(realpath /var/lib/tinydm/default-session.desktop)" = /usr/share/wayland-sessions/phosh.desktop ]; then
	tinydm-set-session -f -s /usr/share/xsessions/phosh-nested.desktop
else
	tinydm-set-session -s /usr/share/xsessions/phosh-nested.desktop
fi

exit 0
